<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <changeSet id="2026-01-28-0006-message_reads" author="gfedorov">
        <preConditions onFail="MARK_RAN">
            <not>
                <tableExists tableName="message_reads"/>
            </not>
        </preConditions>

        <createTable tableName="message_reads">
            <column name="id" type="BIGINT" autoIncrement="true">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="message_id" type="BIGINT">
                <constraints nullable="false"/>
            </column>
            <column name="user_id" type="BIGINT">
                <constraints nullable="false"/>
            </column>
            <column name="read_at" type="TIMESTAMP" defaultValueComputed="CURRENT_TIMESTAMP"/>
        </createTable>

        <addUniqueConstraint
                tableName="message_reads"
                columnNames="message_id, user_id"
                constraintName="uk_message_reads_message_user"/>

        <addForeignKeyConstraint
                baseTableName="message_reads"
                baseColumnNames="message_id"
                referencedTableName="messages"
                referencedColumnNames="id"
                constraintName="fk_message_reads_message"
                onDelete="CASCADE"/>

        <addForeignKeyConstraint
                baseTableName="message_reads"
                baseColumnNames="user_id"
                referencedTableName="users"
                referencedColumnNames="id"
                constraintName="fk_message_reads_user"
                onDelete="CASCADE"/>

        <createIndex tableName="message_reads" indexName="idx_message_reads_message_id">
            <column name="message_id"/>
        </createIndex>

        <createIndex tableName="message_reads" indexName="idx_message_reads_user_id">
            <column name="user_id"/>
        </createIndex>

        <rollback>
            <dropTable tableName="message_reads"/>
        </rollback>
    </changeSet>

</databaseChangeLog>